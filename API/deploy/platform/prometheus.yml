global:
  scrape_interval: 10s
rule_files:
  - /etc/prometheus/alerts.yml
scrape_configs:
  - job_name: commerce
    metrics_path: /metrics
    dns_sd_configs:
      - names: [orders, customers, products]
        type: A
        port: 8000
        refresh_interval: 10s
