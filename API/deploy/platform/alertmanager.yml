route:
  receiver: observe-agent
  group_by: [alertname, service]
  group_wait: 10s
  group_interval: 1m
  repeat_interval: 30m
receivers:
  - name: observe-agent
    webhook_configs:
      - url: http://observe-agent:8090/v1/alerts
        send_resolved: true
