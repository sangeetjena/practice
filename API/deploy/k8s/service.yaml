apiVersion: v1
kind: Service
metadata:
  name: api-interview-lab
  namespace: api-interview
spec:
  selector:
    app: api-interview-lab
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP

