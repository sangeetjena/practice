# Run from ObserveAgent: gcloud builds submit --config deploy/gcp/cloudbuild.yaml --substitutions _IMAGE=REGION-docker.pkg.dev/PROJECT/REPOSITORY/observe-agent:TAG
steps:
  - name: gcr.io/cloud-builders/docker
    args: [build, -f, Dockerfile.cloud, -t, '${_IMAGE}', .]
images: ['${_IMAGE}']
