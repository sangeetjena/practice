```
O(1) DS design classics

146. LRU Cache — get/put O(1) using HashMap + Doubly Linked List
Time: O(1) each, Space: O(capacity)

460. LFU Cache — get/put O(1) using freq lists + HashMap
Time: O(1) each, Space: O(capacity)

380. Insert Delete GetRandom O(1) — set with random
Time: avg O(1), Space: O(n)

381. Insert Delete GetRandom O(1) - Duplicates allowed
Time: avg O(1), Space: O(n)

432. All O`one Data Structure — inc/dec/getMax/getMin O(1)
Time: O(1), Space: O(n)

895. Maximum Frequency Stack — push/pop O(1)
Time: O(1), Space: O(n)

Stack/Queue with O(1) operations

225. Implement Stack using Queues — amortized O(1) (common solution)

232. Implement Queue using Stacks — amortized O(1) pop/peek

622. Design Circular Queue — O(1) operations

641. Design Circular Deque — O(1) operations

“Get min/max in O(1)” family

716. Max Stack — supports peekMax/popMax (often needs TreeMap; not pure O(1) for all ops, but asked a lot)

496. Next Greater Element I / 503. Next Greater Element II — monotonic stack (overall O(n), but each element pushed/popped once ⇒ amortized O(1) per element)

O(1) key/value + counting patterns

359. Logger Rate Limiter — near O(1) per call

362. Design Hit Counter — O(1) or O(log n) depending approach

705. Design HashSet / 706. Design HashMap — core DS design

```
